<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test">

    <!--网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:allowBackup="true"
        android:name="com.feng.kotlin.AppContext"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Pitch">
        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <!--协议部分-->
                <data android:scheme="pitch"
                    android:host="pitch.com"
                    android:pathPrefix="/test/"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.ProfileActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.HomeActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.EditActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.ForgetActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.ConfirmEditActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.ResetActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.VideoRecyclerviewActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.VlcPlayerActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.ChartsActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.MpChartsActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.QRCodeActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.AudioActivity" android:launchMode="singleTask"/>
        <activity android:name=".ui.AudioListActivity" android:launchMode="singleTask"/>

        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="com.azuresamples.msalandroidapp"
                    android:path="/1wIqXSqBj7w+h11ZifsnqwgyKrY="
                    android:scheme="msauth" />
            </intent-filter>
        </activity>
    </application>

</manifest>